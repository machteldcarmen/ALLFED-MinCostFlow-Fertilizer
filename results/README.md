# results

Outputs generated by the notebooks and
`src.postprocessing.save_result(...)`: CSVs of the solved flow matrix
and per-country summary tables.

This folder is **git-ignored** (except for the `.gitkeep` placeholder)
so generated files don't end up in version control. Re-generate by
running the notebooks in `../scripts/`.
