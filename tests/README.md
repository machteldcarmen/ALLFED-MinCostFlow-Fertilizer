# tests

Unit tests. Run locally with:

```bash
pytest
```

Also run on every push via `../.github/workflows/testing.yml`.

| File            | What it covers                                                                 |
|-----------------|--------------------------------------------------------------------------------|
| `test_mcf.py`   | Feasibility, demand satisfaction, mass balance, rebalance residuals, error handling, shock impact, summary table columns |
